@model IEnumerable<IbhayiPharmacy.Models.Medication>

@{
    ViewData["Title"] = "Stock Management";
    Layout = "~/Views/Shared/_ManagerLayout.cshtml";
}

<div class="container">
    <div class="card p-3 mb-4">
        <h5 class="title-underline">Stock Management</h5>
        <input type="text" class="form-control" placeholder="Search medication...">
    </div>

    <div class="card p-3">
        <h6 class="title-underline">Medication Stock Levels</h6>

        <table class="table table-hover table-borderless align-middle">
            <thead class="table-light" style="color: #2c3e50;">
                <tr>
                    <th>Medication</th>
                    <th>Dosage Form</th>
                    <th>Quantity on Hand</th>
                    <th>Re-order Level</th>
                    <th>Set Stock</th>
                    <th>Increment</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var med in Model)
                {
                    <tr>
                        <td>@med.MedicationName</td>
                        <td>@med.DosageForm?.DosageFormName</td>
                        <td>@med.QuantityOnHand</td>
                        <td>@med.ReOrderLevel</td>
                        <td colspan="3">
                            <form asp-action="UpdateStock" method="post" class="d-flex align-items-center gap-2">
                                <input type="hidden" name="id" value="@med.MedcationID" />

                                <input type="number" name="newStock" class="form-control form-control-sm" placeholder="Set stock (e.g. 150)" style="width: 130px;" />
                                <input type="number" name="increment" class="form-control form-control-sm" placeholder="Add (e.g. 20)" style="width: 100px;" />

                                <button type="submit" class="btn btn-sm btn-success">Apply</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
